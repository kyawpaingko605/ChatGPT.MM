name: Upload Python Package

on:
  release:
    types: [published]
  push:
    branches: [main, master]
    paths:
      - 'src/**'
      - '*.py'
      - 'test/**'
      - '.github/workflows/**'
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main, master]
    paths:
      - 'src/**'
      - '*.py'
      - 'test/**'
      - '.github/workflows/**'
  workflow_dispatch: # လက်ရှိမျက်နှာပြင်မှာ ကိုယ်တိုင်နှိပ်ပြီး Run လို့ရမယ့် ခလုတ်

permissions:
  contents: read

jobs:
  ci:
    if: github.event_name != 'release'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.11"]
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: make
      - name: Build CI
        run: make build
      - name: Syntax CI
        run: make ci

  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: make
    - name: Build package
      run: make build
    - name: Publish package
      uses: pypa/gh-action-pypi-publish@v1.12.4
      with:
        user: __token__
        password: ${{ secrets.PYPI_API_TOKEN }}
